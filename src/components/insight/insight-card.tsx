import Image from "next/image";
import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import type { Insight } from "@/content/insights";

export function InsightCard({ insight, featured = false }: { insight: Insight; featured?: boolean }) {
  return <article className={`group flex h-full flex-col overflow-hidden rounded-2xl border border-ink-950/10 bg-white transition-all hover:-translate-y-1 hover:border-signal-500/40 hover:shadow-[0_20px_55px_rgba(8,17,28,.09)] ${featured?"md:grid md:grid-cols-[1.15fr_.85fr]":""}`}><Link href={`/insights/${insight.slug}`} className={`relative block overflow-hidden bg-ink-950 ${featured?"min-h-80":"aspect-[16/10]"}`}><Image src={insight.image} alt={insight.alt} fill sizes={featured?"(min-width:768px) 58vw,100vw":"(min-width:1024px) 33vw,100vw"} className="object-cover transition-transform duration-700 group-hover:scale-[1.04]"/></Link><div className="flex flex-1 flex-col p-6 md:p-7"><div className="flex items-center justify-between gap-4"><p className="eyebrow text-signal-600">{insight.category}</p><span className="font-mono text-[.65rem] text-slate-500">{insight.readingTime}</span></div><h2 className={`mt-5 font-semibold leading-tight tracking-[-.035em] ${featured?"text-3xl md:text-4xl":"text-2xl"}`}><Link href={`/insights/${insight.slug}`}>{insight.title}</Link></h2><p className="mt-4 text-sm leading-6 text-slate-700">{insight.excerpt}</p><div className="mt-auto flex items-center justify-between border-t border-ink-950/10 pt-5 text-xs text-slate-500"><time dateTime={insight.publishedAt}>{new Intl.DateTimeFormat("en-IN",{day:"numeric",month:"short",year:"numeric"}).format(new Date(`${insight.publishedAt}T00:00:00`))}</time><ArrowUpRight aria-hidden="true" className="transition-transform group-hover:translate-x-1 group-hover:-translate-y-1" size={18}/></div></div></article>;
}
