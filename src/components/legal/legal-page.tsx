import Link from "next/link";

export type LegalSection = { title: string; paragraphs: string[]; items?: string[] };

export function LegalPage({ eyebrow, title, summary, updated, sections }: { eyebrow: string; title: string; summary: string; updated: string; sections: LegalSection[] }) {
  return <><header className="bg-ink-950 py-20 text-white md:py-28"><div className="container-shell"><p className="eyebrow text-signal-500">{eyebrow}</p><h1 className="mt-7 max-w-5xl text-5xl font-semibold tracking-[-.06em] md:text-7xl">{title}</h1><p className="mt-7 max-w-3xl text-lg leading-8 text-steel-300">{summary}</p><p className="mt-8 font-mono text-xs uppercase tracking-[.12em] text-slate-500">Last updated {updated}</p></div></header><div className="bg-paper-50 py-16 md:py-24"><div className="container-shell grid gap-12 lg:grid-cols-[15rem_minmax(0,48rem)] lg:justify-center lg:gap-16"><aside className="hidden lg:block"><nav className="sticky top-28" aria-label={`${title} sections`}><p className="eyebrow text-slate-500">On this page</p><ol className="mt-5 space-y-3 border-l border-ink-950/15 pl-4">{sections.map((section, index) => <li key={section.title}><a className="text-sm text-slate-700 hover:text-signal-600" href={`#legal-${index + 1}`}>{section.title}</a></li>)}</ol></nav></aside><div>{sections.map((section, index) => <section id={`legal-${index + 1}`} key={section.title} className="scroll-mt-28 border-t border-ink-950/15 py-10 first:border-0 first:pt-0"><p className="eyebrow text-signal-600">{String(index + 1).padStart(2, "0")}</p><h2 className="mt-4 text-3xl font-semibold tracking-[-.035em]">{section.title}</h2>{section.paragraphs.map((paragraph) => <p key={paragraph} className="mt-5 leading-7 text-slate-700">{paragraph}</p>)}{section.items && <ul className="mt-5 list-disc space-y-2 pl-5 leading-7 text-slate-700">{section.items.map((item) => <li key={item}>{item}</li>)}</ul>}</section>)}<div className="mt-8 rounded-2xl bg-mist-100 p-6"><h2 className="text-xl font-semibold">Questions about this document?</h2><p className="mt-2 text-sm leading-6 text-slate-700">Use the contact page and select “General enquiry” so the request can be routed appropriately.</p><Link className="button button-primary mt-5" href="/contact">Contact Corvex</Link></div></div></div></div></>;
}
